info:
  name: CATEGORIES - CREATE
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/categories"
  body:
    type: json
    data: "{\r

      \  \"name\": \"Otros Egresos\",\r

      \  \"type\": \"as\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
