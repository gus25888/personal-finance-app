info:
    name: MOVEMENTS - GET ONE
    type: http
    seq: 8

http:
    method: GET
    url: "{{baseUrl}}/movements/1"
    body:
        type: json
        data: ""
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
