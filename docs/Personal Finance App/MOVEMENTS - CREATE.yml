info:
  name: MOVEMENTS - CREATE
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/movements"
  body:
    type: json
    data: |-
      {
        "description": "Nuevo Ingreso 4",
        "date": "2026-01-12",
        "amount": 15000,
        "category": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
